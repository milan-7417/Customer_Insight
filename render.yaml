services:
  - type: web
    name: customerinsight
    env: python
    buildCommand: pip install -r requirements.txt && python utils/data_generator.py
    startCommand: streamlit run app.py --server.port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
